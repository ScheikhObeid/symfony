{% extends 'admin/base_admin.html.twig' %}

{% block title %}Edit Service{% endblock %}

{% block admin_body %}
<h1 class="text-2xl font-bold text-gray-800 mb-6">✏️ Edit Service</h1>

<div class="bg-white p-6 rounded-lg shadow-md max-w-2xl">
    {{ include('admin/service/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="mt-6 flex justify-between items-center">
        <a href="{{ path('app_service_index') }}" class="text-blue-600 hover:underline text-sm font-medium">
            ← Back to list
        </a>

        {{ include('admin/service/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}