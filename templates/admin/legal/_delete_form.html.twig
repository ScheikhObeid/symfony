<form method="post" action="{{ path('app_admin_legal_delete', {'id': legal.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ legal.id) }}">
    <button class="btn">Delete</button>
</form>
